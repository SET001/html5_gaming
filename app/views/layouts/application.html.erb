<!DOCTYPE html>
<html lang="en">
<head>
  <meta http-equiv="Content-Type" content="text/html;charset=utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="<%= image_path("favicon.ico") %>" rel="shortcut icon" />
  <title>HTML5 Gaming ;)</title>
  <%= stylesheet_link_tag "vendors", :media => "all" %>
  <%= stylesheet_link_tag "application", :media => "all" %>
  <%= csrf_meta_tags %>
</head>

<body>
  <div id="wrapper" ng-app="app" ng-controller="AppCtrl">

    <%= render 'header' %>

    <div class="container">
      <div class="btn-group">
        <button ng-click="add_unit()" class="btn btn-default"><span class="glyphicon glyphicon-plus-sign"></span> Add unit</button>
    
        <dutton ng-click="settings()" class="btn btn-default"><span class="glyphicon glyphicon-cog"></span> Settings...</button>
      </div>
      <div class="row">
        <div class="col-lg-9">
          <div id="container">
            <game></game>
          </div>
        </div>
        <div class="col-lg-3">
          <dl class="dl-horizontal">
            <dt>Map size:</dt><dd>{{game.config.map_width}}x{{game.config.map_height}}</dd>
            <dt>Units:</dt><dd>{{game.units.length}}</dd>
          </dl>

          <ul class="list-inline">
            <li><a href class="btn btn-primary add_unit" title="Add unit"></a></li>
            <li><a href title="Remove unit"></a></li>
            <li><a href class="btn btn-primary add_stone" title="Add stone">
            </a></li>
          </ul>
        </div>
      </div>

      <div class="row">
        {{foo}}
        <div class="col-lg-12">
          <p>Map is dragable if world size is bigger than vizible part.</p>
        </div>
        <p><a href="https://github.com/SET001/html5_gaming">Source on GitHub</a></p>
      </div>

    </div>
    <%= render 'footer' %>

  <%= javascript_include_tag "vendors" %>
  <%= javascript_include_tag "application" %>
</body>
</html>
